dependencies {
  compile "org.apache.tomcat:catalina:${versions.tomcat6}"

  runtime "org.apache.tomcat:coyote:${versions.tomcat6}",
          "org.apache.tomcat:jasper:${versions.tomcat6}"

  testCompile "org.apache.httpcomponents:httpclient:${versions.httpClient}"

}
